#include <stdio.h>

int main(){
    printf("Este programa retornará todos los divisores de un numero entero dado.");
    printf("\nIngrese un numero entero: ");
    int num; scanf("%d", &num);
    for(int i; i < num; i++)
        if (num % divisor == 0)
        {
            printf("%d", divisor);
        }else
        {

    
    return 0;
}